<flow-definition plugin="workflow-job@1385.vb_58b_86ea_fff1">
  <actions>
    <org.jenkinsci.plugins.pipeline.modeldefinition.actions.DeclarativeJobAction plugin="pipeline-model-definition@2.2175.v76a_fff0a_2618" />
    <org.jenkinsci.plugins.pipeline.modeldefinition.actions.DeclarativeJobPropertyTrackerAction plugin="pipeline-model-definition@2.2175.v76a_fff0a_2618">
      <jobProperties />
      <triggers />
      <parameters />
      <options />
    </org.jenkinsci.plugins.pipeline.modeldefinition.actions.DeclarativeJobPropertyTrackerAction>
  </actions>
  <description>Build build containers for linux_x86_64.&lt;!-- Managed by Jenkins Job Builder --&gt;</description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <org.jenkinsci.plugins.workflow.job.properties.DisableConcurrentBuildsJobProperty>
      <abortPrevious>false</abortPrevious>
    </org.jenkinsci.plugins.workflow.job.properties.DisableConcurrentBuildsJobProperty>
    <jenkins.model.BuildDiscarderProperty>
      <strategy class="hudson.tasks.LogRotator">
        <daysToKeep>-1</daysToKeep>
        <numToKeep>10</numToKeep>
        <artifactDaysToKeep>-1</artifactDaysToKeep>
        <artifactNumToKeep>-1</artifactNumToKeep>
      </strategy>
    </jenkins.model.BuildDiscarderProperty>
    <hudson.model.ParametersDefinitionProperty>
      <parameterDefinitions>
        <hudson.model.StringParameterDefinition>
          <name>BUILDNODE</name>
          <description>The node to run docker build on.</description>
          <defaultValue>agent-linux-x86_64-1</defaultValue>
          <trim>false</trim>
        </hudson.model.StringParameterDefinition>
        <hudson.model.BooleanParameterDefinition>
          <name>PULL</name>
          <description>Set docker build option --pull</description>
          <defaultValue>false</defaultValue>
        </hudson.model.BooleanParameterDefinition>
        <hudson.model.BooleanParameterDefinition>
          <name>NO_CACHE</name>
          <description>Set docker build option --no-cache</description>
          <defaultValue>false</defaultValue>
        </hudson.model.BooleanParameterDefinition>
      </parameterDefinitions>
    </hudson.model.ParametersDefinitionProperty>
  </properties>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsFlowDefinition" plugin="workflow-cps@3853.vb_a_490d892963">
    <script>pipeline {
    agent {
        label "${BUILDNODE}"
    }
    stages {
        stage("Checkout Infrastructure Repository") {
            steps {
                checkout changelog: false, poll: false, scm: [$class: 'GitSCM', branches: [[name: '*/master']], extensions: [[$class: 'RelativeTargetDirectory', relativeTargetDir: 'SapMachine-infrastructure']], userRemoteConfigs: [[credentialsId: 'SapMachine-github', url: 'https://github.com/SAP/SapMachine-infrastructure.git']]]
            }
        }
        stage("Build Ubuntu container") {
            steps {
                withCredentials([usernameColonPassword(credentialsId: 'openjdk-artifactory', variable: 'ARTIFACTORY_CREDS')]) {
                    script {
                        def pull = params.PULL == true ? '--pull ' : ''
                        def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                        docker.build('ubuntu_20_04-x86_64', pull + no_cache + '--build-arg ARTIFACTORY_CREDS=\"$ARTIFACTORY_CREDS\" SapMachine-infrastructure/dockerfiles/ubuntu_20_04/x86_64/.')
                    }
                }
            }
        }
        stage("Build Async build container") {
            steps {
                script {
                    def pull = params.PULL == true ? '--pull ' : ''
                    def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                    docker.build('async-build-x86_64', pull + no_cache + 'SapMachine-infrastructure/dockerfiles/async/x86_64/.')
                }
            }
        }
        stage("Build Devkit build container") {
            steps {
                script {
                    def pull = params.PULL == true ? '--pull ' : ''
                    def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                    docker.build('devkit-build-x86_64', pull + no_cache + 'SapMachine-infrastructure/devkits/dockerfiles/x86_64/.')
                }
            }
        }
        stage("Build Alpine container") {
            steps {
                script {
                    def pull = params.PULL == true ? '--pull ' : ''
                    def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                    docker.build('alpine_3-x86_64', pull + no_cache + 'SapMachine-infrastructure/dockerfiles/alpine_3/x86_64/.')
                }
            }
        }
        stage("Build Infrastructure container") {
            steps {
                script {
                    def pull = params.PULL == true ? '--pull ' : ''
                    def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                    docker.build('infrastructure-x86_64', pull + no_cache + 'SapMachine-infrastructure/dockerfiles/infrastructure/.')
                }
            }
        }
        stage("Build JTReg build container") {
            steps {
                script {
                    def pull = params.PULL == true ? '--pull ' : ''
                    def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                    docker.build('jtreg-build-x86_64', pull + no_cache + 'SapMachine-infrastructure/dockerfiles/jtreg-build/.')
                }
            }
        }
        stage("Build OSSRH upload container") {
            steps {
                script {
                    def pull = params.PULL == true ? '--pull ' : ''
                    def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                    docker.build('ossrh-upload-x86_64', pull + no_cache + 'SapMachine-infrastructure/dockerfiles/ossrh-upload/.')
                }
            }
        }
        stage("Build RPM container") {
            steps {
                script {
                    def pull = params.PULL == true ? '--pull ' : ''
                    def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                    docker.build('fedora-rpm-x86_64', pull + no_cache + 'SapMachine-infrastructure/dockerfiles/fedora-rpm/.')
                }
            }
        }
        stage("Build JMC container") {
            steps {
                script {
                    def pull = params.PULL == true ? '--pull ' : ''
                    def no_cache = params.NO_CACHE == true ? '--no-cache ' : ''
                    docker.build('jmc-x86_64', pull + no_cache + 'SapMachine-infrastructure/dockerfiles/jmc/x86_64/.')
                }
            }
        }
        stage("Prune images") {
            steps {
                script {
                    sh 'docker image prune -f'
                }
            }
        }
        stage("Prune builder") {
            steps {
                script {
                    sh 'docker builder prune -f'
                }
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
</script>
    <sandbox>true</sandbox>
  </definition>
  <disabled>false</disabled>
</flow-definition>