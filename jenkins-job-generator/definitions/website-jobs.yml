- job:
    name: generate-website-data
    description: 'Update dynamic website data.'
    project-type: pipeline
    sandbox: true
    concurrent: false
    properties:
        - build-discarder:
            num-to-keep: 40
    parameters:
        - string:
            name: SAPMACHINE_VERSION
            default: ''
            description: 'The SapMachine version to update data. If empty, all releases are scanned.'
    dsl: |
        pipeline {
            agent {
                label "website"
            }
            stages {
                stage("Checkout Infrastructure Repository") {
                    steps {
                        checkout changelog: false, poll: false, scm: [$class: 'GitSCM', branches: [[name: '*/master']], extensions: [[$class: 'RelativeTargetDirectory', relativeTargetDir: 'SapMachine-infrastructure']], userRemoteConfigs: [[url: 'https://github.com/SAP/SapMachine-infrastructure.git']]]
                    }
                }
                stage('Generate Data') {
                    agent {
                        dockerfile {
                            dir "SapMachine-infrastructure/dockerfiles/infrastructure"
                            reuseNode true
                            label "website"
                        }
                    }
                    steps {
                        withCredentials([[$class: 'UsernamePasswordMultiBinding', credentialsId: 'SapMachine-github', usernameVariable: 'GIT_USER', passwordVariable: 'GIT_PASSWORD']]) {
                            lock('ReleaseDataGenerationLock') {
                                sh '''
                                    if [ -z "$SAPMACHINE_VERSION" ]; then
                                        python3 SapMachine-infrastructure/lib/generate_release_data.py --no-status -o git
                                    else
                                        python3 SapMachine-infrastructure/lib/generate_release_data.py --no-status -o git -t ${SAPMACHINE_VERSION}
                                    fi
                                '''
                            }
                        }
                    }
                }
            }
            post {
                always {
                    cleanWs()
                }
            }
        }
- job:
    name: generate-website-data-jmc
    description: 'Update dynamic website data for jmc.'
    project-type: pipeline
    sandbox: true
    concurrent: false
    properties:
        - build-discarder:
            num-to-keep: 40
    dsl: |
        pipeline {
            agent {
                label "website"
            }
            stages {
                stage("Checkout Infrastructure Repository") {
                    steps {
                        checkout changelog: false, poll: false, scm: [$class: 'GitSCM', branches: [[name: '*/master']], extensions: [[$class: 'RelativeTargetDirectory', relativeTargetDir: 'SapMachine-infrastructure']], userRemoteConfigs: [[url: 'https://github.com/SAP/SapMachine-infrastructure.git']]]
                    }
                }
                stage('Generate Data') {
                    agent {
                          dockerfile {
                              dir "SapMachine-infrastructure/dockerfiles/infrastructure"
                              reuseNode true
                              label "website"
                          }
                      }
                      steps {
                          withCredentials([[$class: 'UsernamePasswordMultiBinding', credentialsId: 'SapMachine-github', usernameVariable: 'GIT_USER', passwordVariable: 'GIT_PASSWORD']]) {
                              sh "python3 SapMachine-infrastructure/lib/generate_website_data_jmc.py"
                          }
                      }
                  }
            }
            post {
                always {
                    cleanWs()
                }
            }
        }
